monitoring:
    interval: 10
    system:
        cpu:
            icon: cpu
            threshold: 90
        memory:
            icon: speedometer
            threshold: 90
        title: LMON Dashboard
web:
    host: 127.0.0.1
    port: 0
webhook:
    enabled: true
    url: http://localhost:8080/test_webhook
